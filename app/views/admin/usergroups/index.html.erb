<%#= Usergroup controller %>
<% roundbox2 do -%>
 <%= render :partial => 'shared/admin/panel_table', :locals => {:per_page => @usergroups.per_page} %>
  <% form_for 'tasks', :url => usergroup_url('destroy'), :html =>{:method => :delete, :name=>'adminForm'} do -%>
    <%= hidden_field_tag 'task','search' -%>
    <% show_model_table(@usergroups,
      [['name','Имя группы']] , :class => 'grid',
      :cellpadding => 0, :cellspacing => 0, :border => 0,
      :numeration => '№', :checkbox => 'ids[]'
    ) do |row,counter|  -%>
      <tr class="<%= cycle('odd','even') %>"<%= 'id="after_edit"' if flash[:edited].to_s == row.id.to_s %> >
      <%= content_tag(:td, counter, :align => "center", :width => "2%") %>
      <%= content_tag(:td, check_box_tag('ids[]',row.id), :align => "center", :width => "2%" ) %>
      <%= content_tag(:td, link_to(row.name,:id => row.id, :action => 'edit')) %>
      </tr>
    <% end %>
    <%= render :partial =>'shared/admin/toolbar_controller', :locals => { :paginated_rows => @usergroups, :icon_class => 'icon_user_group_48' } %>
  <% end %>
<% end %>
<%= javascript_tag(visual_effect(:highlight,:after_edit, :duration => 1.3)) if flash[:edited] %>
