<table class="table table-striped table-bordered table-condensed">
<thead>
  <th>#</th>
  <th><%= check_box_tag("groups", false, false, :onclick=>"setCheckBoxes(this,'ids[]')") %></th>
  <th><%= sortable('Название','name') %></th>
</thead>
<tbody>
<% @sections.map do |row| %>
  <tr>
    <th width="2%"><%= row.id -%></th>
    <th width="1%"><%= check_box_tag('ids[]',row.id) -%></th>
    <td><%= link_to(row.name.html_safe, edit_admin_section_url(row.id)) %></td>
  </tr>
<% end %>
</tbody>
</table>