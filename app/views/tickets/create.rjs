if current_user
  page.insert_html 'bottom', 'opened_ticket_body', :partial => 'tickets/ticket_row', :locals => {:ticket => @new_ticket}
  page.call('showtab', 'link_history', 'ticket_history' )
else
  page['ticket_email'].value = ''
end
page['new_ticket_subject'].value = ''
page['ticket_messages_message'].value = ''

page.call 'Flash.notice', "Сообщение отправленно! Спасибо за внимание!"
